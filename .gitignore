bags/
!insight_capture/postprocess/bags/
!insight_capture/postprocess/bags/**
logs/
scripts/__pycache__/
__pycache__/
*.py[cod]
build/
*.egg-info/
node_modules/
.env
integrations/openclaw-workspace/openclaw-workspace-state.json

# Runtime/recording data and release build artifacts -- never tracked, but
# also weren't actually gitignored (relying on nobody running `git add -A`).
release/
rosbags/
outputs/
runs/
data/

# Local hand-pose research workspace. Production source lives in
# scripts/handpose; recordings stay in rosbags and generated data in outputs.
gesture/

# Per-device config, generated by scripts/select_device.sh from
# config/devices/<name>/ -- no longer hand-edited per branch.
config/cameras.json
config/post_processing.json
config/runtime.json
config/.device
